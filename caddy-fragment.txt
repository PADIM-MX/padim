# Fragmento para agregar al final de /etc/caddy/Caddyfile
# PADIM — padim.enmexico.casa (v2.1.0)
# Ya existe el A record: padim.enmexico.casa → 74.208.40.123

padim.enmexico.casa {
    root * /home/polaris/workspace/projects/BACKBONE/padim/site
    encode gzip zstd

    # Aviso legal (extensionless URL)
    handle /aviso-legal {
        rewrite * /aviso-legal.html
        file_server
    }

    # Términos de servicio (extensionless URL)
    handle /terminos {
        rewrite * /terminos.html
        file_server
    }

    # Docs de la especificación
    handle /docs/* {
        root * /home/polaris/workspace/projects/BACKBONE/padim/docs
        file_server
    }

    # Redirect a GitHub
    handle /repo {
        redir https://github.com/PADIM-MX/padim permanent
    }

    # Archivos estáticos (robots.txt, sitemap.xml, index.html, etc.)
    handle {
        file_server
    }

    header {
        X-Content-Type-Options "nosniff"
        X-Frame-Options "DENY"
        -Server
    }
}